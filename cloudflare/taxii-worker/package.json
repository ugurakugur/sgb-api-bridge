{
  "name": "sgb-taxii-worker",
  "version": "1.0.0",
  "private": true,
  "description": "SGB TAXII 2.1 server on Cloudflare Workers — fronts static TAXII tree on GitHub Pages.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.85.0"
  }
}
