apiVersion: v1
kind: Service
metadata:
  name: usom-bridge
  namespace: usom-bridge
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: usom-bridge
    app.kubernetes.io/component: serve
  ports:
    - name: http
      port: 80
      targetPort: http
